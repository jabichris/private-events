<h5><%= previous_created? %></h5>
  <% unless @user.past_created_events.count.zero? %>
    <ul>
    <% @user.past_created_events.each do |event| %>
      <li><%= link_to event.title, event %> </li>
    <% end %>
    </ul>
  <% end %>